# Optional: force a specific decoration image instead of the daily rotation.
# Accepted values: soulLeavingBody | candlelightDark | shy | blossomBurst
VITE_DECORATION_IMAGE=

# Server-side only (set these in the Vercel dashboard, never commit them).
# Required by /api/analytics.ts to proxy Vercel Web Analytics.
# VERCEL_TOKEN=
# VERCEL_PROJECT_ID=
# VERCEL_TEAM_ID=
